import { Payment } from "../Models/paymentModel.js"

export const orders = ((req,res)=>{
    const {
        paymentID,
        totalCost,
        paymentDate,
        paymentMethod,
        status,
        
       
    } = req.body

})

export const verify = ((req,res)=>{

})